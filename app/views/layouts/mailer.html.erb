<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
      @import url("https://use.typekit.net/zhb5abj.css");

      * {
        margin: 0;
      }

      h1, h2, h3, h4, p, li {
        font-family: 'objektiv-mk1', sans-serif;
        color: #242933;
      }

      a  {
        font-family: 'gemeli mono', monospace;
        color: #242933;
      }

      .border-40 {
        width: 32px;
        height: 4px;
        border: 0;
        background: #242933;
        margin: 8px 0;
      }

      .padding-left {
        padding-left: 16px;
      }

      .confirmation-mail {
        background: #A9CDC9;
        padding: 24px 24px 48px 24px;
      }

      .email-header {
        margin: 24px 0 24px;
      }

      .email-header p {
        margin-top: 16px;
      }

      .commande-item {
        margin: 24px 0;
      }

      .commande-title {
        font-family: 'gemeli mono', monospace;
      }

      .commande-detail {
        font-family: 'objektiv-mk1', sans-serif;
      }

      .commande-note {
        margin: 8px 0;
      }

      .commande-note p{
        font-size: 12px;
      }

      .email-white {
        background: white;
        font-family: 'gemeli mono', monospace;
        padding: 0 2px;
      }

      @media (min-width: 1200px) {
        h1 {
          font-size: 48px;
        }

        h4  {
          font-size: 32px;
        }
      }

    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
